
let CRp = {};

CRp.drawNodeIcon = function (context, ele, shiftToOriginWithBb, force, prefix, useEleOpacity = true) {
  context.moveTo(0, 0);
  context.lineTo(100, 100);
  context.stroke();
};

export default CRp;
